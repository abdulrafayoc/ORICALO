# ASR Training (Whisper)

This folder is intended for scripts and notebooks related to fine-tuning or
adapting Whisper (or similar ASR models) for Urdu telephony data.

Suggested contents:

- Data preparation scripts (manifest generation, splitting).
- Fine-tuning scripts using Hugging Face `Trainer` or custom loops.
- Evaluation scripts for comparing baseline vs fine-tuned models.

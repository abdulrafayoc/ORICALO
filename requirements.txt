# ============================================================================
# ORICALO: Dual-Backend Dependencies
# ============================================================================

# --- Core API Dependencies (High-Speed PoC) ---
groq>=0.4.0                 # Groq LLM API
openai>=1.0.0               # OpenAI API (Fallback)
deepgram-sdk>=3.2.0         # Deepgram STT API
elevenlabs>=0.2.26          # ElevenLabs TTS API
edge-tts>=6.1.9             # Free Edge TTS API

# --- Core Local Dependencies (FYP Defense) ---
torch>=2.0.0                # PyTorch for model inference
transformers>=4.30.0        # HuggingFace Transformers (Whisper models)
accelerate>=0.20.0          # Optimized model loading
sentencepiece>=0.1.99       # Tokenization for some models
pysbd>=0.3.4                # Sentence boundary detection
llama-cpp-python>=0.2.20    # Local LLM Inference

# --- Shared Web & Audio Dependencies ---
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
websockets>=12.0
pyaudio>=0.2.13             # Microphone input and audio I/O
sounddevice>=0.4.6          # Alternative audio backend
librosa>=0.10.0             # Audio file loading and processing
scipy>=1.10.0               # Signal processing utilities
pydub>=0.25.1               # Audio format conversion
numpy>=1.24.0               # Numerical computing
pandas>=2.0.0               # Data manipulation

# --- Shared RAG & DB Dependencies ---
chromadb>=0.4.0             # Vector Database
sentence-transformers>=2.2.0 # Text Embeddings
sqlalchemy>=2.0.0
alembic>=1.11.0

# --- Evaluation & Utilities ---
jiwer>=3.0.0                # Word Error Rate (WER) calculation
scikit-learn>=1.2.0         # Additional metrics and analysis
python-dotenv>=1.0.0        # Environment variable management
tqdm>=4.65.0                # Progress bars for evaluations
httpx>=0.24.0               # Async HTTP client
twilio>=9.0.0               # Telephony Integration
prometheus-fastapi-instrumentator>=7.0.0 # Observability
pytest>=8.0.0               # Testing framework

# Note:
# - On Windows, you may need to install PyAudio using: pip install pipwin && pipwin install pyaudio
# - On Linux, install portaudio first: sudo apt-get install portaudio19-dev
# - On macOS: brew install portaudio
